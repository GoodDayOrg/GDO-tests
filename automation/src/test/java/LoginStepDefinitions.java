import io.cucumber.java.en.Then;

public class LoginStepDefinitions {
    @Then("Login button is displayed on home page")
    public void loginButtonIsDisplayedOnHomePage() {






        //        WebElement loginButton = driver.findElement(By.className("btn-outline-primary"));
//        String expected = "Login";
//        String actual =  loginButton.getText();
//        Assertions.assertEquals(expected,actual);

        //loginButton.click();

    }
}
